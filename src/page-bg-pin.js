/** iOS: keep full-page background static — do NOT tie height to visualViewport (resizes while scrolling). */
export function initPageBgPin() {
  /* Background size is CSS-only: fixed stack + overscanned image (see styles.css). */
}
